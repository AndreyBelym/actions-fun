name: TestCafe Npm Workflow
on: [push]

jobs:
  build:
    name: TestCafe Git Job
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v1
      - name: Run testcafe from 'npm'
        uses: AlexKamaev/testcafe-action@master
        with:
          args: "chrome tests"
